// the main goal was to accomplish the animation
//builds a random word from scrath, based on lose logic, and some routing
// if word ends in silent e
// if it ends in ey
// if it ends in es
// if it ends in ion
// if it ends

var massiveArray = [32, 265, 5413, 23, 6, 456, 8, 456, 12, 145, 98];
var vowel = ['a', 'e', 'i', 'o', 'u', 'y'];
var consenant = ['b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n', 'p', 'q', 'r', 's', 't', 'v', 'w', 'x', 'y', 'z'];
var wordLogicArray = [];
var wordArray = [];

function wordbuilder() {
  var switcher = random() * 200; /*randomly assigns a number*/
  if (switcher % 2 === 0)
  vowel.push = random() * 600;
  } else {
    vowel.consenant = random() * 600;
    switcher = random() * 200;
  }

  function = takeRandomWords() {
    wordArray.push() = @@@;
  }


  }

}
function randomAZ();
var doIt
if (vowel, expect consenant,
  if consenant, expect vowel) {

var randomSetStartIndex = random(1000);
var twoFifty = 250;
Var chunk = function saveMemor() {
  if chunk




  for (i = randomSetStartIndex; i < randomSetStartIndex; i++) {
    console.log(randomSetStartIndex);
    console.log(randomSetStartIndex);
  }
}
